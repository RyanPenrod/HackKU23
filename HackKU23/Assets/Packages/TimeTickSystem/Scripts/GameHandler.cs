using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using CodeMonkey;
//using CodeMonkey.Utils;
//using CodeMonkey.MonoBehaviours;

public class GameHandler : MonoBehaviour {
    /*

    private void Start() {
        //new Building(Vector3.zero);
        
        TimeTickSystem.OnTick += delegate (object sender, TimeTickSystem.OnTickEventArgs e) {
            CMDebug.TextPopupMouse("tick: " + e.tick);
        };
        TimeTickSystem.OnTick_5 += delegate (object sender, TimeTickSystem.OnTickEventArgs e) {
            CMDebug.TextPopupMouse("MEGATICK");
        };
    }

    private void Update() {
        if (Input.GetMouseButtonDown(0)) {
            new Building(UtilsClass.GetMouseWorldPosition(), 30);
        }
    }
    */
}
